module.exports = function(config) {
  config.set({
    basePath: '',
    files: [
      // Defined by gulpdsa
    ],
    frameworks: ['jasmine'],
    singleRun: true,
    browsers: [ 'Chrome' ]
  });
};
